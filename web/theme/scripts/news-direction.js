// Generated by CoffeeScript 1.9.3
(function() {
  jQuery(function($) {
    var AddBlogInnerButton, doctorAddBlogWrapper, hoverAddContainer;
    $("#newsMore").click(function(e) {
      var direction, directionUrl, offset, side;
      offset = $("#newsOffset");
      side = $("#newsSide");
      direction = $("#newsDirection");
      directionUrl = $(this).data('href');
      e.preventDefault();
      e.stopPropagation();
      return $.getJSON(directionUrl, {
        offset: offset.val(),
        side: side.val()
      }, (function(_this) {
        return function(resp) {
          $(resp.mores).appendTo($("#newsContainer")).slideDown();
          offset.val(parseInt(offset.val()) + 3);
          side.val(function() {
            if (side.val() === '0') {
              return 1;
            } else {
              return 0;
            }
          });
          if (!resp.larger) {
            return $(_this).hide();
          }
        };
      })(this));
    });
    AddBlogInnerButton = $(".header-blog-inner-button");
    doctorAddBlogWrapper = $(".addDoctor-blog-hover-wrapper");
    hoverAddContainer = ".hover-add-container";
    AddBlogInnerButton.click(function() {
      doctorAddBlogWrapper.show();
      return AddBlogInnerButton.toggleClass("activeBlogInner");
    });
    return doctorAddBlogWrapper.click(function(e) {
      if (!$(e.target).parents().is(hoverAddContainer)) {
        $(this).hide();
      }
      return AddBlogInnerButton.toggleClass("activeBlogInner");
    });
  });

}).call(this);

//# sourceMappingURL=news-direction.js.map
